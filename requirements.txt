# Polotto Engenharia - Calculo Estrutural
# VERSOES FIXADAS (deploy estavel). O Streamlit Cloud, com ">=", instalava
# sempre o mais novo de tudo (pyarrow 25 / streamlit 1.59 no Python 3.14 em
# Linux) e isso causava "Segmentation fault" (crash nativo). Estas versoes sao
# as testadas e funcionam tanto em Python 3.12 quanto 3.14.
streamlit==1.58.0
numpy==2.5.0
pandas==3.0.3
matplotlib==3.11.0
pyarrow==24.0.0
PyMuPDF==1.28.0
